<%- include ("template/_header") %>

<div class="container mt-5">
  <div class="row">
    <div class="col-md-5 offset-md-3 card">
      <div class="card-body">
        <form action="/update/<%= data.id %>" method="POST">
          <div class="form-group">
            <input type="text" name="nombre" value="<%= data.nombre%>" placeholder="Nombre de la Tarea" class="form-control" autofocus autocomplete="off">
          </div>
          <div class="form-group">
            <input type="text" name="descripcion" value="<%= data.descripcion%>" placeholder="Descripción de la Tarea" class="form-control" autocomplete="off">
          </div>

          <button type="submit" class="btn btn-primary">
            Guardar Cambios
          </button>
        </form>
      </div>
    </div>
  </div>
</div>

<%- include ("template/_footer")%>
